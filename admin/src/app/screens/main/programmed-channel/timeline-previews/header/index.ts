import { ClassComponent } from 'mithril';
import { template } from './template';

export class TimelinePreviewsHeader implements ClassComponent {
  public view() {
    return template.call(this);
  }
}
